# Public API used by the browser (REST + Socket.IO). Required in production builds (e.g. Vercel).
VITE_API_URL=http://localhost:4000

# Optional: Sentry DSN for error reporting
# VITE_SENTRY_DSN=
# VITE_SENTRY_TRACES_SAMPLE_RATE=0.1
