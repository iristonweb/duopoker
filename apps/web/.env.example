# Full realtime + voice: point to Render or local Express backend (REST + Socket.IO).
# Leave empty only for Vercel serverless fallback (polling, no voice).
VITE_API_URL=http://localhost:4000

# Optional: Sentry DSN for error reporting
# VITE_SENTRY_DSN=
# VITE_SENTRY_TRACES_SAMPLE_RATE=0.1
