# =============================================================================
# DuoPoker — локальная разработка (корень репо, справочник)
# =============================================================================
# Рабочие секреты храни в apps/backend/.env и packages/db-schema/.env
# На Vercel используй .env.vercel.example
# =============================================================================

# См. apps/backend/.env.example      — Express + Socket.IO (:4000)
# См. apps/web/.env.example          — Vite frontend (:5180)
# См. packages/db-schema/.env.example — pnpm db:push
# См. .env.vercel.example            — деплой на Vercel
