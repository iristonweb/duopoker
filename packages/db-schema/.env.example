# =============================================================================
# Prisma — только для pnpm db:push / db:migrate локально
# =============================================================================
# Скопируй в packages/db-schema/.env (тот же DATABASE_URL что на Vercel/Neon)
# =============================================================================

DATABASE_URL=postgresql://duopoker:duopoker@127.0.0.1:5434/duopoker
