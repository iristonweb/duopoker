/** Pause after hand completes before auto-dealing the next hand. */
export const NEXT_HAND_DELAY_MS = 2800;

/** Time allowed for a human player to act before auto-fold. */
export const ACTION_TIMEOUT_MS = 60_000;
